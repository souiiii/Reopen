# Reopen Terms

Reopen is provided as productivity software for macOS.

Use Reopen with workspaces and commands you trust. Terminal commands run with the same user permissions you have in macOS, so review saved commands before launching a workspace.

Window layout restore is best-effort because macOS and individual apps control which windows can be moved or resized.
